# Setup Scripts

Environment setup and sanity checks.

```bash
bash scripts/setup/check_setup.sh
bash scripts/setup/setup_cluster_env.sh
```

The cluster setup script prepares the `.venv` used by Slurm jobs.
